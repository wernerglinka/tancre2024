{#
  Text
  Renders a modal text section with optional image and CTAs
  
  params: {
    text: {
      prefix: string - prefix to the title (optional)
      title: string - title of the blog post (optional)
      header: string - header of the blog post, e.g. "h1", "h2", etc. (optional)
      subtitle: string - subtitle of the blog post (optional)
      prose: string - prose of the blog post (optional)
    }
    ctas: {
      url: "",
      label: "",
      isExternal: true,
      isButton: true,
      buttonStyle: "primary"
    }
    image: {
      src: "",
      alt: "",
      caption: ""
    }
  }

  YAML example:
  hasCtas: false
  text:
    prefix: ""
    title: ""
    header: ""
    subtitle: ""
    prose: ""
  ctas:
    - url: ""
      label: ""
      isExternal: true
      isButton: true
      buttonStyle: "primary"

  image:
      src: ""
      alt: ""
      caption: ""
#}

{% from "sections/particles/text.njk" import text %}
{% from "sections/particles/ctas.njk" import ctas %}
{% from "sections/particles/image.njk" import image %}

<div class="modal-wrapper">
  <span class="modal-close">CLOSE</span>
  <div class="modal-content">
    {% if params.image.url %}
    {{ image(params) }}
    {% endif %}
    {{ text(params) }}
    {{ ctas(params) }}
  </div>
</div>